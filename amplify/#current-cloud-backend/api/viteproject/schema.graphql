type Room @model @auth(rules: [{allow: private}]) {
  id: ID!
  branchName: String
  branchId: String
}

type Message @model @auth(rules: [{allow: owner, operations: [read, create, delete]}, {allow: private, operations: [read]}]) {
  id: ID!
  content: String!
  roomId: String
  type: Int
}
 